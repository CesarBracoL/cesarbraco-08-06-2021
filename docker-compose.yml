version: '3.4'

services:
  web:
    environment:
      - ASPNETCORE_URLS=http://*:5000;
    ports:
      - 8082:5000
    build:
      context: .
      dockerfile: Dockerfile
      args: 
        - APP_ENVIRONMENT=Development
      labels:
        application: WebApp